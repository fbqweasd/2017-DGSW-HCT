using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SAGAM : MonoBehaviour {
	/*float rnd_room;
	float rnd_time;
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		rnd_room = Random.Range (1, 7);
		rnd_time = Random.Range (5.0f, 15.0f);
		if(rnd_room==1)
			this.transform.position(

	}*/
}
